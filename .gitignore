# macOS
.DS_Store

# Xcode - per-user state
xcuserdata/
*.xcuserstate

# Xcode - build artifacts
build/
DerivedData/
*.xcworkspace/xcshareddata/swiftpm/.build/

# Swift Package Manager
.swiftpm/
.build/

# Sparkle CLI tools, if downloaded locally (only generate_keys is ever needed
# by hand — the release workflow fetches its own copy)
Tools/sparkle/
