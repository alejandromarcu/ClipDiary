# GitHub Pages root

This folder is served by GitHub Pages at
`https://alejandromarcu.github.io/ClipDiary/` (repo Settings → Pages →
deploy from branch, `main` + `/docs`).

- `appcast.xml` — the Sparkle update feed the app checks
  (regenerated by `Tools/release.sh`, committed per release).
- A future download page is just an `index.html` added here.
